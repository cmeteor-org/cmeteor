(function(uploader) {
	$(document).ready(function() {
		// var options = {
		// 	domain: 'http://7xjbiz.com1.z0.glb.clouddn.com',
		// 	tokenUrl: '/cmeteor-token'
		// }
		// var ins = new uploader(options);

		// $('#upload').on('change', function() {
		// 	var file = this.files[0];
		// 	ins.post(file, function(err, result) {
		// 		if (err) {
		// 			console.log(err)
		// 		}
		// 		console.log(result)
		// 	});
		// 	console.log(ins.file.total, ins.file.loaded)
		// });
	});
})(window.fontQnUper)